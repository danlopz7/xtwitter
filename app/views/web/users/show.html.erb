<p style="color: green"><%= notice %></p>
<%= render partial: 'user_details', locals: { user: @user } %>
<h2>Tweets</h2>
<%= render partial: 'web/tweets/tweet', collection: @tweets_with_stats, as: :tweet_data %>
<%= link_to "Back to all users", web_tweets_path %>
